# Design Implementation Plan: Humanize (Inverse Manifesto)

## Summary
- **Scope:** Full-page single-page app overhaul.
- **Target:** `public/index.html`, `public/styles.css`, `public/app.js`
- **Winner variant:** Variant C (Inverted Manifesto)
- **Key improvements:** Editorial typography hierarchy, high-contrast inverted palette, tactile grain overlay, and split-pane output.

## Files to Change
- [ ] `public/styles.css` - Complete rewrite for manifesto aesthetics.
- [ ] `public/index.html` - Structure overhaul to support hero typography and new layout.
- [ ] `public/app.js` - Update DOM selectors for the new layout and add processing hints.

## Implementation Steps
1. **Define Root Variables:** Add Fraunces and Manrope fonts, set `--color-canvas: #e8e6e1` and `--color-paper: #0a0a0c`.
2. **Apply Global Grain:** Inject SVG noise filter and overlay with `opacity: 0.03`.
3. **Hero Statement:** Implement "SAY_IT_HUMAN" in `Fraunces` weight 100, size `15vw`.
4. **Input Buffer:** Create a high-contrast container with `#f5f4f0` background and thin border.
5. **Output Section:** Large scale typography (`3rem`) for the humanized result.
6. **Animations:** Subtle fade-in and vertical translation for section entry.

## Component API (CSS-driven)
- `.manifesto-container`: Max-width `1200px` centering.
- `.text-area-wrapper`: Subtle contrast background for inputs.
- `.v-output`: Editorial scale typography for the result.
- `.btn-large`: High-contrast primary action button.

## Required UI States
- **Loading:** Subtle opacity pulsing on the hero statement.
- **Empty:** Placeholder text in Fraunces, low opacity.
- **Error:** Accent color (`#d4c5a2`) used for inline warnings.
- **Copy Feedback:** Instant transformation of button label to "COPIED_TO_CLIPBOARD".

## Accessibility Checklist
- [x] Fraunces/Manrope contrast ratio > 4.5:1.
- [x] Focus rings on text areas.
- [x] Aria-live regions for transformation status.
- [x] Semantic `<main>` and `<section>` tags.

---
*Generated by Gemini CLI Design Lab*
