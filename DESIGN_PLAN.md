# Design Implementation Plan: Humanize App Icon

## Summary
- **Scope:** component (application icon)
- **Target:** `Resources/AppIcon-1024.png` and derived macOS icon assets
- **Winner variant:** E (Before/After Notebook), refined by removing the top-left mini bubble
- **Key improvements:** clearer before/after copy transformation story, approval signal, cleaner top edge silhouette

## Files to Change
- [x] `Resources/AppIcon-1024.png` - approved master icon artwork
- [x] `Resources/AppIcon.iconset/*` - generated size variants for macOS icon pipeline
- [x] `Resources/AppIcon.icns` - compiled app icon bundle
- [x] `scripts/generate-app-icons.sh` - source-driven icon generation from master PNG

## Implementation Steps
1. Select final concept from design lab and apply requested refinements.
2. Promote approved PNG as source of truth at `Resources/AppIcon-1024.png`.
3. Generate full iconset and `AppIcon.icns` from source PNG using `sips` + `iconutil`.
4. Validate bundle inclusion via `scripts/build-app.sh` and verify `CFBundleIconFile` linkage.

## Component API
- **Script:** `scripts/generate-app-icons.sh`
- **Flags:**
  - `--source <path>` override source PNG
  - `--help` usage information

## Required UI States
- N/A (icon asset task)

## Accessibility Checklist
- [x] High-contrast silhouette at small sizes
- [x] Distinct focal shape (notebook + check badge)
- [x] Avoids tiny decorative elements that reduce legibility

## Testing Checklist
- [x] `swift build`
- [x] `swift test`
- [x] `bash scripts/build-app.sh`
- [x] Verify icon file exists in app bundle resources

## Design Tokens
- Light palette alignment:
  - `sky100`: `#E0F2FE`
  - `sky200`: `#BAE6FD`
  - `sky500`: `#0EA5E9`
  - `sky600`: `#0284C7`
  - `sky700`: `#0369A1`
  - neutral paper/line tones from existing UI theme

---

*Generated by Design Lab workflow*
