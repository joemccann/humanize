#if os(macOS)
import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// The global hotkey that toggles the Humanize floating panel.
    static let togglePanel = Self("toggleHumanizePanel")
}
#endif
